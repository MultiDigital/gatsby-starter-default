# Disable problematic prebuilt binaries
msgpackr_prebuilds=false

# Use npm ci for faster, reliable builds
package-lock=true

# Cache configuration
cache=/tmp/.npm

# Disable optional dependencies that might cause issues
optional=false

# Increase timeout for slow networks
timeout=60000 